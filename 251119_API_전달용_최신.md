# BlogDex 등급 조회 API

## 🌐 접속 URL
```
https://doberman-safe-heron.ngrok-free.app
```

---

## 📝 API 엔드포인트

### 1. 단일 URL 조회
```http
POST /api/blog/grade
```

**요청:**
```json
{
  "url": "https://blog.naver.com/nightd/224041403656"
}
```

**응답:**
```json
{
  "url": "https://blog.naver.com/nightd/224041403656",
  "blog_id": "nightd",
  "grade": "최적2+",
  "level": "엑스퍼트3",
  "tier": "엑스퍼트 블로거",
  "timestamp": "2025-11-19 20:15:30",
  "success": true,
  "file_path": "data/json_results/nightd_grade_20251119_201530.json"
}
```

---

### 2. 다수 URL 일괄 조회
```http
POST /api/blog/grades
```

**요청:**
```json
{
  "urls": [
    "https://blog.naver.com/blog1/123",
    "https://blog.naver.com/blog2/456"
  ]
}
```

**응답:**
```json
[
  {
    "url": "https://blog.naver.com/blog1/123",
    "blog_id": "blog1",
    "grade": "최적5",
    "level": "엑스퍼트3",
    "tier": "엑스퍼트 블로거",
    "timestamp": "2025-11-19 20:15:30",
    "success": true,
    "file_path": "data/json_results/blog1_grade_20251119_201530.json"
  },
  {
    "url": "https://blog.naver.com/blog2/456",
    "level": null,
    "success": false,
    "error": "등급 추출 실패"
  }
]
```

---

## 🚀 사용 예시

### Python
```python
import requests

response = requests.post(
    "https://doberman-safe-heron.ngrok-free.app/api/blog/grade",
    json={"url": "https://blog.naver.com/nightd/224041403656"},
    headers={"ngrok-skip-browser-warning": "true"}
)

result = response.json()
print(f"등급: {result['level']}")
print(f"티어: {result['tier']}")
```

### JavaScript (Node.js)
```javascript
const axios = require('axios');

axios.post(
    'https://doberman-safe-heron.ngrok-free.app/api/blog/grade',
    { url: 'https://blog.naver.com/nightd/224041403656' },
    { headers: { 'ngrok-skip-browser-warning': 'true' } }
).then(response => {
    console.log('등급:', response.data.level);
    console.log('티어:', response.data.tier);
});
```

### cURL
```bash
curl -X POST https://doberman-safe-heron.ngrok-free.app/api/blog/grade \
  -H "Content-Type: application/json" \
  -H "ngrok-skip-browser-warning: true" \
  -d '{"url":"https://blog.naver.com/nightd/224041403656"}'
```

---

## ⚠️ 중요 사항

### 응답 시간
- **단일 요청**: 30-40초 소요
- **다수 요청**: 최대 3개 동시 처리 (3개: 30-40초, 5개: 60-80초)

### 헤더 필수
프로그래밍 방식 사용 시 반드시 헤더 추가:
```json
"ngrok-skip-browser-warning": "true"
```

### 동시 처리 제한
- 최대 3개 URL 동시 처리
- 초과 시 대기 큐에 추가

---

## 📊 응답 필드 설명

| 필드 | 타입 | 설명 |
|------|------|------|
| `url` | string | 요청한 블로그 URL |
| `blog_id` | string | 블로그 ID |
| `grade` | string | 원본 등급 (예: "최적2+") |
| `level` | string | 매핑된 레벨 (예: "엑스퍼트3") |
| `tier` | string | 티어 (예: "엑스퍼트 블로거") |
| `timestamp` | string | 조회 시각 |
| `success` | boolean | 성공 여부 |
| `error` | string | 에러 메시지 (실패 시) |
| `file_path` | string | 저장된 파일 경로 |

---

## 📚 추가 정보

### API 문서 (Swagger UI)
```
https://your-ngrok-url.ngrok-free.app/docs
```

### Health Check
```
GET https://your-ngrok-url.ngrok-free.app/health
```

**응답:**
```json
{
  "status": "ok",
  "message": "BlogDex Grade API is running",
  "max_concurrent": 3
}
```

---

## 💡 참고

- 모든 크롤링 결과는 서버에 JSON 파일로 자동 저장됩니다
- 저장 위치: `data/json_results/{blog_id}_grade_{timestamp}.json`
- 성공/실패 모두 기록됩니다
